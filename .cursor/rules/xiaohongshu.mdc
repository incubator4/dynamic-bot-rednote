---
description: 小红书接入必须保守：Cookie 登录、限速、风控停手、不落盘密钥
alwaysApply: true
---

# 小红书接入

平台标识固定为 `rednote`。对外文案用「小红书」。

- 登录支持用户提供的 Cookie，以及网页扫码登录（ADR-0010）；不要把真实 Cookie、抓包 HAR、会话文件提交进仓库。
- 默认关闭轮询或使用不少于 60s 的间隔；连续请求间隔不少于 1s。
- 未登录、Cookie 失效、疑似风控：暂停轮询，不要加密重试或绕过校验。
- 启动默认不补发旧笔记（补发窗口默认 0）。
- 订阅键用小红书用户 ID；不要默认扫全站或对每个用户高频打个人接口。直播只轮询启用了开播/下播事件的订阅用户，间隔与笔记检测相同。
- 文档和代码只写本插件如何对接 dynamic-bot，不写利用平台漏洞或未授权访问的步骤。
- 链接解析（若实现）覆盖笔记、用户主页和 `xhslink.com` 短链，失败时返回可理解的中文原因。
